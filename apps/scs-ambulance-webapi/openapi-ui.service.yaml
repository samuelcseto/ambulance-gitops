kind: Service
apiVersion: v1
metadata:
    name: scs-openapi-ui
spec:
    selector:
        pod: scs-ambulance-webapi-label
    ports:
        - name: http
          protocol: TCP
          port: 80
          targetPort: 8081
