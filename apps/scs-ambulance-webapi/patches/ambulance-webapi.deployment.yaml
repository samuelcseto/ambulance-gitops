apiVersion: apps/v1
kind: Deployment
metadata:
    name: scs-ambulance-webapi
spec:
    template:
        spec:
            containers:
                - name: openapi-ui
                  env:
                      - name: URL
                        value: /scs-openapi
                      - name: BASE_URL
                        value: /scs-openapi-ui
